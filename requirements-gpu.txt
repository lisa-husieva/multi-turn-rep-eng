# Install this on the GPU machine (Linux + CUDA)
# Usage:
#   pip install -r requirements-gpu.txt   # installs vllm + its pinned torch
#   pip install -r requirements.txt       # pip keeps existing torch, installs everything else
vllm==0.8.3
