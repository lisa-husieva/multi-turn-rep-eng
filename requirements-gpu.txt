# Install this on the GPU machine (Linux + CUDA)
# Do NOT install requirements.txt first — vllm manages torch itself.
# Usage: pip install -r requirements-gpu.txt && pip install -r requirements.txt --no-deps torch
vllm==0.8.3
